package class5;

public class LogicalOperators {
    public static void main(String[] args) {
        /*
        NOT (!) it flips the boolean values [true false flip]
        AND (&&)
        OR (||)
         */
        boolean flag = true;
        System.out.println(!flag);
        System.out.println(flag);
    }
}
